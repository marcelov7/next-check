# Arquivo exemplo de variáveis de ambiente para produção
# Não comite suas credenciais reais no repositório. Use o painel do CloudPanel para setar as variáveis.

# Ambiente
NODE_ENV=production
PORT=3000

# String de conexão com o MySQL (substitua os placeholders)
# ATENÇÃO: se a senha contém caracteres especiais (ex: @, %, /) faça URL-encode da senha antes de colocar aqui.
# Formato: mysql://USUARIO:SENHA_URLENCODED@HOST:PORT/NOME_DB
DATABASE_URL="mysql://userparada:M%40rcelo1809%403033@31.97.168.137:3306/dbparada"

# NextAuth configuration
# NEXTAUTH_URL deve usar https no ambiente de produção (após habilitar SSL no CloudPanel)
NEXTAUTH_URL="https://www.checkparada.devaxis.com.br"
# NEXTAUTH_SECRET: string forte, por exemplo gerada por: openssl rand -hex 32
NEXTAUTH_SECRET="mude-para-uma-string-secreta-forte-aqui"

# Observações:
# - Configure essas variáveis no painel do CloudPanel (Environment / Environment Variables) para não deixar arquivos .env em produção.
# - Se precisar gerar a versão URL-encoded da senha, rode localmente:
#   python3 -c "import urllib.parse; print(urllib.parse.quote('SUA_SENHA_AQUI'))"
# - Para gerar NEXTAUTH_SECRET (exemplo):
#   openssl rand -hex 32
# - Se vier a usar upload de arquivos, verifique permissões do diretório de uploads e considere usar armazenamento externo (S3) em produção.
